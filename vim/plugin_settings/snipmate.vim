" Doc {
  "Used to set the autor name
  "let g:snips_author = 'Javo'
  "
  "
  "Default trigger is tab, it can be changed
  "let g:snips_trigger_key='<c-space>'
  "Default trigger backward is <S-tab>, it can also be changed
  "let g:snips_trigger_key_backwards='<c-space>'
" }

" Options {
  let g:snips_author = 'Javo'
" }

" Mappings {
" }

